export default class Frames
{
    constructor(framesPerState)
    {
        this.framesPerState = framesPerState;   //Numero de frames por estado de animacion
        this.frameCounter   = 1;                //Contador de frames
    }
}